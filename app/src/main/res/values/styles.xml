<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- 弹出框动画 由下至上 -->
    <style name="dialogstyle_vertical" mce_bogus="1" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/slide_in_from_bottom</item>
        <!-- 进入时的动画 -->
        <item name="@android:windowExitAnimation">@anim/slide_out_to_bottom</item>
        <!-- 退出时的动画 -->
    </style>
    <!-- 弹出框动画 缩放 -->
    <style name="dialogstyle_scale" mce_bogus="1" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/slide_in_from_amplification</item>
        <!-- 进入时的动画 -->
        <item name="@android:windowExitAnimation">@anim/slide_out_to_narrow</item>
        <!-- 退出时的动画 -->
    </style>
</resources>
